# Snippet Summary Frontend

## Getting Started

```bash
npm install
```
First, run the development server:

```bash
npm run dev
```

Open [http://localhost:4000](http://localhost:4000) with your browser to see the result.

or run with docker

```bash
docker build -t myfront .
```

```bash
docker run -p 4000:4000 myfront
```
